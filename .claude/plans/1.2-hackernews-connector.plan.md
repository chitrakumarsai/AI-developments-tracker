# Plan — Subphase 1.2 Slice 5: Hacker News connector

**PRD:** [`1.2-hackernews-connector.prd.md`](../prds/1.2-hackernews-connector.prd.md) · Reuses the `api` host router. TDD throughout (≥80% new code). **GATE 1** (approved) → **GATE 2** (pre-commit).

## Key insight
Third **provider** under the existing `api` type — no new registry entry. Keyless API (simpler than GitHub/HF: no auth header, no missing-token warning). The only novel bit is the **link-first fallback**: text posts have no external `url`, so fall back to the HN discussion page.

## Build order (test-first)
1. **Fixture** — `__fixtures__/hn-search.json`: external-url story (HTML entity in title), Ask HN text post (null url → fallback), missing-title skip, missing-id+url skip.
2. **Tests** — `hackernews.test.ts`: pure mapper (mapping, fallback, sanitize, skip+warn), connector (keyless fetch/no auth header, non-200 warn, reject warn, SSRF refusal).
3. **Connector** — `hackernews.ts`: `parseHnSearch` (pure) + `hackernewsConnector` (SSRF guard → keyless fetch → parse). Warn-not-throw.
4. **Router** — add `hn.algolia.com` host branch.
5. **Seed** — two `api` rows (query=AI, query=LLM; points>=100).
6. **Verify** — full vitest + tsc + lint; then live ingest run.

## Files touched
```
web/lib/ingestion/api/hackernews.ts                  (new)
web/lib/ingestion/api/hackernews.test.ts             (new)
web/lib/ingestion/api/__fixtures__/hn-search.json    (new)
web/lib/ingestion/api/router.ts                      (mod — hn.algolia.com branch)
supabase/seed.sql                                    (mod — 2 HN rows)
```

## Skills / agents
`ingestion-connector` + ECC `api-connector-builder` (pattern); review: `code-reviewer` + **`security-reviewer`** (untrusted ingestion — mandatory §12.5).

## GATE 2 ask
Present diff + review; commit only on approval.
